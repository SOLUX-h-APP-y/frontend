import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: '80%',
    height: 60,
    borderRadius: 50,
    justifyContent: 'center',
  },
  text: {
    textAlign: 'center',
    fontSize: 15,
    fontWeight: 'bold',
  },
});
